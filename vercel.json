{
  "buildCommand": "npx expo export -p web",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/house/:id", "destination": "/house/[id].html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
